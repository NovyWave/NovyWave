#!/bin/bash
# SessionStart Hook - Initialize task timing for new sessions

source "$(dirname "$0")/shared-functions.sh"
init_hook_env

# Record session start time
TASK_START_TIME=$(date +%s)
echo "🚀 New session started: $(date)" >> "$HOOK_LOG"

# Store start time for statusline
echo "$TASK_START_TIME" > "$PROJECT_ROOT/.claude/task-start-time"